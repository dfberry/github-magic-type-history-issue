export const QUERY_ORG_REPOS_AGGREGATE_VARIABLES: Record<string, unknown> = {
  organization: 'Azure-Samples',
  after: '',
  pageSize: 100
}

export const QUERY_ORG_REPOS_AGGREGATE = `
  
  `
